the app and vis files are web apps.

running the app: `python server.py pathto/deck.pptx --annotator annotator-name`

running the vis: `python compare_server.py --decks pathto/dataset/tasks --responses pathto/dataset/responses --out compare-work-folder`
